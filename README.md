# swift-CGit2

[libgit2](https://libgit2.github.com/) module for [swift](https://swift.org/).

This assumes the libgit2 prefix is `/usr/local`.

Which is what it would be if installed via [Homebrew](http://brew.sh/) with the following:
```shell
$ brew install libgit2
```
